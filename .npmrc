# The project pins @storybook/addon-essentials@8.x alongside storybook@10.x.
# npm's strict peer resolution rejects that skew, so resolve peers leniently
# the same way the project has always been installed locally.
legacy-peer-deps=true
